<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <meta http-equiv="X-UA-Compatible" content="ie=edge">
    <title>Document</title>
    <link rel="stylesheet" href="https://stackpath.bootstrapcdn.com/bootstrap/4.4.1/css/bootstrap.min.css"
        integrity="sha384-Vkoo8x4CGsO3+Hhxv8T/Q5PaXtkKtu6ug5TOeNV6gBiFeWPGFN9MuhOf23Q9Ifjh" crossorigin="anonymous">
    <link href="https://fonts.googleapis.com/css?family=Passion+One&display=swap" rel="stylesheet">
    <link rel="stylesheet" type="text/css" href="../public/assets/css/style.css" />
</head>

<body>
    <div class="container">
        <h1 class="text-center mb-3 mt-5">Burger Logger</h1>

        <div class="row">
            <div class="col text-center mb-2">
                <form class="add-burger">
                    <div class="form-group">
                        <input class="form-control-sm" id="new-burger" type="text" placeholder="Add a burger" name="name">
                        <button type="submit" class="btn btn-primary">Grill It</button>
                    </div>
                </form>
            </div>
        </div>

        <div class="row">
            <div class="col">
                <div class="card text-center mb-5">
                    <h2 class="mb-2 mt-3">Waiting to be devoured</h2>
                    <img width="525" class="bun-top" src="/assets/images/bun-top.png">
                    <div class="burger-body">
                        <ul>
                            {{!-- <li> --}}
                            {{#each burgers}}
                            {{#unless devoured}}
                            {{> burgers/burger-block devoured=true}}
                            {{/unless}}
                            {{/each}}
                            {{!-- </li> --}}
                        </ul>
                    </div>
                    <img width="525" class="bun-bottom" src="/assets/images/bun-bottom.png">
                </div>
            </div>
            <div class="col">
                <div class="card text-center mb-5">
                    <h2 class="mb-2 mt-3">Devoured!</h2>
                    <img width="525" class="bun-top" src="/assets/images/bun-top.png">
                    <div class="burger-body">
                        <ul>
                            {{!-- <li> --}}
                            {{#each burgers}}
                            {{#if devoured}}
                            {{> burgers/burger-block devoured=false}}
                            {{/if}}
                            {{/each}}
                            </li>
                        </ul>
                    </div>
                    <img width="525" class="bun-bottom" src="/assets/images/bun-bottom.png">
                </div>
            </div>
        </div>
    </div>
</body>

</html>